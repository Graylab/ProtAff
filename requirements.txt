# Minimal dependencies for ProtAff inference (tools/predict.py).
# For training/analysis, see environment-full.yml.
torch>=2.1,<3
transformers>=4.40,<5
peft>=0.10
safetensors>=0.4
omegaconf>=2.3
pandas>=2.0
tqdm>=4.60
pyyaml>=6.0
