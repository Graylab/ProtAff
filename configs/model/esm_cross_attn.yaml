# configs/model/esm_cross_attn.yaml
name: "facebook/esm2_t33_650M_UR50D"
arch: "cross_attn"
max_length: 1024

# Decoder Parameters (Simple/Standard)
n_heads: 8
d_model: 256
dropout: 0.3

# LoRA
lora:
  r: 8
  alpha: 16
  dropout: 0.3
  target_modules: ["query", "key", "value"]
  modules_to_save: []
