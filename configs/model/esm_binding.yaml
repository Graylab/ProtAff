# configs/model/esm_binding.yaml
name: "facebook/esm2_t33_650M_UR50D"
max_length: 1024

# Decoder Parameters
d_model: 256
n_heads: 8
n_cross_layers: 2
dropout: 0.1

# LoRA
lora:
  r: 16
  alpha: 32
  dropout: 0.1
  target_modules: ["query", "key", "value"]
  modules_to_save: []
  n_last_layers: 4
