# configs/model/esm_interaction_map.yaml
name: "facebook/esm2_t33_650M_UR50D"
arch: "interaction_map"
max_length: 1024

# Decoder Parameters (Simple/Standard)
n_heads: 8
d_model: 256
dropout: 0.1

# LoRA
lora:
  r: 8
  alpha: 16
  dropout: 0.1
  target_modules: ["key", "value"]
  modules_to_save: []
  n_last_layers: 4
